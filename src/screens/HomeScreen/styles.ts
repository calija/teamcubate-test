import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  content: {
    gap: 20,
    paddingTop: 20,
  },
  headerTextContainer: {
    alignSelf: 'center',
  },
});

export default styles;
